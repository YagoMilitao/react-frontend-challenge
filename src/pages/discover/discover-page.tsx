/**
 * Placeholder da página de descoberta.
 * Será substituída na próxima etapa (dashboard de descoberta: listagem,
 * busca com debounce, filtros e paginação).
 */
export function DiscoverPage() {
  return (
    <div className="container py-8">
      <h1 className="text-2xl font-semibold">Descobrir filmes</h1>
      <p className="mt-2 text-muted-foreground">
        Em construção — próxima etapa do desafio.
      </p>
    </div>
  );
}
